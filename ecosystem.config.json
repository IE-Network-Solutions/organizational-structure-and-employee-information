{
  apps: [
    {
      name: "osei-service-1",
      script: "server.js",
      args: "--port=8001",
      instances: 1,
      exec_mode: "fork",
      autorestart: true,
      restart_delay: 5000,
      env: {
        NODE_ENV: "production",
        PORT: 8001
      }
    },
    {
      name: "osei-service-2",
      script: "server.js",
      args: "--port=8002",
      instances: 1,
      exec_mode: "fork",
      autorestart: true,
      restart_delay: 5000,
      env: {
        NODE_ENV: "production",
        PORT: 8002
      }
    }
  ]
}
